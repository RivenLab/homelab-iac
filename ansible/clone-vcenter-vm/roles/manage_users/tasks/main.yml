---
- name: Wait for SSH to be available
  ansible.builtin.wait_for:
    host: "{{ inventory_hostname }}"
    port: 22
    delay: 10
    timeout: 300
    state: started
  delegate_to: localhost

- name: Gather facts
  ansible.builtin.setup:
  when: ansible_facts['os_family'] is not defined

- name: Remove packer user if present
  become: true
  ansible.builtin.user:
    name: packer
    state: absent
    remove: yes

- name: Remove /etc/sudoers.d/packer
  become: true
  ansible.builtin.file:
    path: /etc/sudoers.d/packer
    state: absent

- name: Check if user exists
  ansible.builtin.getent:
    database: passwd
    key: "{{ linux_user | default('dragon') }}"
  register: user_check
  failed_when: false
  changed_when: false

- name: Create target user
  become: true
  ansible.builtin.user:
    name: "{{ linux_user | default('dragon') }}"
    uid: 1000
    comment: "Local admin user created by playbook"
    shell: /bin/bash
    groups: "{{ 'wheel' if ansible_facts['os_family'] == 'RedHat' else 'sudo' }}"
    password: "{{ (linux_user_password | default('r')) | password_hash('sha512') }}"
    state: present
    create_home: yes
  when: user_check.ansible_facts.getent_passwd is not defined
  no_log: true

- name: Create .ssh directory for the user
  become: true
  ansible.builtin.file:
    path: "/home/{{ linux_user | default('dragon') }}/.ssh"
    state: directory
    owner: "{{ linux_user | default('dragon') }}"
    group: "{{ linux_user | default('dragon') }}"
    mode: '0700'

- name: Add authorized_key for the user
  become: true
  ansible.builtin.authorized_key:
    user: "{{ linux_user | default('dragon') }}"
    state: present
    key: "{{ linux_ssh_pubkey | default(lookup('file', '~/.ssh/id_ed25519.pub')) }}"
